# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本仓库的完整工程说明以 **`AGENTS.md`** 为唯一基准（目录结构、常用命令、多端 API 基址、IPC 约定、代码约定、注意事项），开始任何工作前先通读它。

补充两条工作方式：

- 仓库语言约定：注释、UI 文案、文档（含 AGENTS.md 与 README 变更记录）均为中文。
- 若 `AGENTS.md` 的描述与当前代码事实不符，以代码为准，并同步修正 `AGENTS.md`，保持其作为唯一基准的准确性。
