node_modules
*.log
.next
# 锚定到仓库根目录：不锚定的 app/dist 会误伤 android/app 等子目录
/app
/dist
.vscode

# TypeScript incremental build cache
*.tsbuildinfo

# API 端点本地配置（模板见 api-server.json.example）
api-server.json
